import React from 'react';
import styles from './styles.module.css';

const CommanderHealthTrackerTitle = () => {
    return <div className={styles.commanderHealthTracker}>
        <h1>Escape Plan's Commander Health Tracker</h1>
    </div>;
};

export default CommanderHealthTrackerTitle; 